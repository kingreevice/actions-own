name: My Workflow

on:
  push:
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest

    steps:

      - name: Get the input variable
        run: echo "The input variable is: ${{ inputs.NUM }}"
